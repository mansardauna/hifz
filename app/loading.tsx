import React from 'react';
import { LoadingSpinner } from '../src/components/ui/LoadingSpinner';

export default function RootLoading() {
  return <LoadingSpinner label="Connecting to Ankabit LMS Platform..." size="full" />;
}
